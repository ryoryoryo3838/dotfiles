# !/bin/bash

# exit immediately
set -e

# the case of debian
# to install brew
# sudo apt install git build essential
# add this sentence to .bashrc↓
# eval "$(/home/linuxbrew/.linuxbrew/bin/brew shellenv)"


# brew
if [[ ! $(command -v brew) ]]; then
	/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
else
	echo "Homebrew  has already been installed"
fi
source $HOME/.bashrc
# install packages in Brewfile with Homebrew
cd $HOME/dotfiles
brew bundle

# setting zsh
# install oh-my-zsh
#if [[ -d "$HOME/.oh-my-zsh" ]]; then
#	echo "oh-my-zsh already exist"
#else
	#sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"
#fi
echo
echo "oh-my-zsh plugins is installing..."
echo
# install zinit (may be optional)
#bash -c "$(curl --fail --show-error --silent --location https://raw.githubusercontent.com/zdharma-continuum/zinit/HEAD/scripts/install.sh)"
# oh-my-zsh plugins
cd ~
#ZSH_Plugins=( \
#	"fdellwing/zsh-bat" \
#	"zsh-users/zsh-syntax-highlighting" \
#	"zsh-users/zsh-autosuggestions" \
	#"z-shell/zsh-eza" \ (なんかうまくいかないのでzinitでいれる
	#"z-shell/zsh-zoxide" \
#)
#for zp in "${ZSH_Plugins[@]}"; do
#	gh_dir="https://github.com/${zp}"
#	plugin_dir="${HOME}/.oh-my-zsh/custom/plugins/${zp##*/}"
#	if [[ ! -d "${plugin_dir}" ]]; then
#		#git clone "${gh_dir}" "${plugin_dir}"
#		echo
#	else
#		echo "${zp##*/} is already installed."
#	fi
#done

#rm $HOME/.zsh* 

# set symlink
stow -d $HOME/dotfiles/ -t $HOME/ zsh 
stow -d $HOME/dotfiles/ -t $HOME/ git
# stow -d $HOME/dotfiles/ -t $HOME/ bash
